{% extends 'base.html.twig' %}

{% block title %}HoodtrooperPlaceComment{% endblock %}

{% block body %}
    <h1>HoodtrooperPlaceComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hoodtrooper_place_comment.id }}</td>
            </tr>
            <tr>
                <th>Comment_text</th>
                <td>{{ hoodtrooper_place_comment.commentText }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hoodtrooper_place_comment_index') }}">back to list</a>

    <a href="{{ path('hoodtrooper_place_comment_edit', {'id': hoodtrooper_place_comment.id}) }}">edit</a>

    {{ include('hoodtrooper_place_comment/_delete_form.html.twig') }}
{% endblock %}
